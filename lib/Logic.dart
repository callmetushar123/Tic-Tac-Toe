import 'package:flutter/material.dart';
import 'package:flutter_icons/flutter_icons.dart';

class Logic {
  Icon choice1(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice2(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice3(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice4(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice5(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice6(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice7(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice8(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }

  Icon choice9(String player) {
    return player == "player1"
        ? Icon(
            Feather.circle,
            color: Color(0xFF39BCD4),
            size: 50,
          )
        : Icon(
            MaterialCommunityIcons.window_close,
            color: Color(0xFF3989D4),
            size: 50,
          );
  }
}
